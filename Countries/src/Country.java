/*@author Subhan Noeman 623424Programming 2Prof Ken Sotak */import java.util.Scanner;public class Country {    public static void main (String []args){        Scanner input = new Scanner ( System.in );        int temp = 0, largestIndex=0, smallestIndex=0, numberOfCountries=0;        System.out.println("Enter number of Countries: ");        numberOfCountries = input.nextInt();        String [] countryNames = new String[numberOfCountries];        String [] capital = new String[numberOfCountries];        int [] population = new int[numberOfCountries];        for ( int i = 0; i<countryNames.length ; i++){            System.out.println("Enter Name for Country #"+i);            countryNames[i] = input.next();        }        for ( int i = 0; i<countryNames.length ; i++){            System.out.println("Enter Population for Country "+countryNames[i]);            population [i] = input.nextInt();            System.out.println("you entered: "+ population[i]);        }        for ( int i = 0; i<countryNames.length ; i++) {            System.out.println("Enter the Capital for " + countryNames[i]);            capital[i] = input.next();            System.out.println("you entered: "+capital[i]);        }        int largestPopulation = population [0];     // min and max values are initialized to the array values        int lowestPopulation = population[0];       // to give a starting point like a clock        // Now that all inputs have been taken from the user, It is time to sort all the data        // and out the data as sorted        for ( int i = 0 ; i< countryNames.length ; i++) {            if (population[i] > largestPopulation) {        //largest is set as array of index 0 value                largestPopulation = population[i];                largestIndex = i;            } else if (population[i] < lowestPopulation) {                lowestPopulation = population[i];                smallestIndex = i;            }        }        System.out.println("The Largest Country is "+ countryNames[largestIndex]+" with population of: "                +largestPopulation);            System.out.println("The Smallest Country is "+ countryNames[smallestIndex]+" with population of: "                    +lowestPopulation);        System.out.println("In least to greatest population size, the countries are: ");        for(int i = 0; i < countryNames.length; i++){            for(int j = i; j < countryNames.length; j++)                if(population[i] > population[j] ){                    temp = population[i];                    population[i]=population[j];                    countryNames[i]= countryNames[j];                    capital[i]=capital[j];                    population[j]=temp;                }            // now that the array has been organised in a ascending order, its time to print it        }        for (int i = 0 ; i<countryNames.length;i++){            System.out.println(countryNames[i]+", " + capital[i]+ " with "+population[i]+" people");        }    }}